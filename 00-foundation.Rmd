# (PART) 基础介绍 {-}

# 介绍 {#chap-foundation .unnumbered}

为什么写这一部分？打算写哪些内容？结构是什么？各个章节的关系是什么？整体上想达到什么目标？



````{comment}
这是评论内容，可以包含代码块。

```{r}
1 + 1
```

上面的代码块内容不会执行，行内的代码，如 `r pi * 5^2` 也会被忽略.
````


````{verbatim}
原样展示内容，内容可以是代码块：

```{r}
1 + 1
```

内容也可以包含行内代码，如 `r pi * 5^2`。
````



